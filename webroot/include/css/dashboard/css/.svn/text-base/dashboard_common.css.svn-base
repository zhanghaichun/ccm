
/* 针对设计图设置按钮的特别样式。 */
/* Default button. */
.db-button-default {
  color: #A5BED6;
  border-color: #DFE2E5;
  border-radius: 1px;
  background: white;
}

.db-button-default:hover {
  border-color: #7094B4;
  color: #7094B4;
  background: white;
}

/* Primary button. */
.db-button-primary {
  color: #fff;
  border-color: #ABC3D9;
  border-radius: 1px;
  background: #ABC3D9;
}

.db-button-primary:hover {
  border-color: #7094B4;
  color: #fff;
  background: #7094B4;
}

/* 内补值比较高的button按钮 */
.wide-button {
  padding: 6px 30px;
}

.narrow-button {
  padding: 6px 8px;
}

/* 设置元素的边框盒为 border-box */
.border-box {
  box-sizing: border-box;
}

/* 设置font size */
.font-12 {
  font-size: 12px;
}

.font-14 {
  font-size: 14px;
}

.font-16 {
  font-size: 16px;
}

.font-18 {
  font-size: 18px;
}

/* 设置字体加粗。 */
.font-bold {
  font-weight: bold;
}

.font-600 {
  font-weight: 600;
}

.font-100 {
  font-weight: 100;
}

/* 设置光标的形状。 */
.pointer {
  cursor: pointer;
}

/* 用户不可选择文本。 */
.select-none {
  -webkit-user-select: none;
     -moz-user-select: none;
      -ms-user-select: none;
          user-select: none;
}

/* 将元素的border, outline 设置为 none, 一般针对 input box. */
.no-border {
  outline: none;
  border: none;
}

/* 在下拉列表中折叠图标的显示。 */
.select-title:after {
  position: absolute;
  content: "";
  width: 16px;
  height: 16px;
  background: url(../img/dropdown-list-down.png) no-repeat;
  background-size: cover;
}

.select-title.foldup:after {
  background: url(../img/dropdown-list-up.png);
}

/* Dashboard 页面中统一的当文字超出时候的效果。 */
.text-overflow {
  font-size: 12px;
  color: #A5BED6;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}